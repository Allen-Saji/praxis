# Master secret for the local Seal stand-in. REQUIRED for the dashboard to
# reveal sealed reasoning. Must match the PRAXIS_SEAL_SECRET the agents sealed
# with. Set this in Vercel project env for the deployed dashboard. Without it,
# decrypt requests fail (reveal throws).
PRAXIS_SEAL_SECRET=
